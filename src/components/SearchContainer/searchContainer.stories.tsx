import { Story, Meta } from '@storybook/react'
import SearchContainer from '.'
export default {
  title: 'SearchContainer',
  component: SearchContainer
} as Meta

export const Default: Story = () => <SearchContainer />
