import { Meta, Story } from '@storybook/react'

import SearchItem from '.'
export default {
  title: 'SearchItem',
  component: SearchItem
} as Meta

export const Default: Story = () => <SearchItem />
