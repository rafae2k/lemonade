import { Meta, Story } from '@storybook/react'

import PlaylistNameModal from '.'
export default {
  title: 'PlaylistNameModal',
  component: PlaylistNameModal
} as Meta

export const Default: Story = () => <PlaylistNameModal />
