import { Meta, Story } from '@storybook/react'

import CreatePlaylistButton from '.'
export default {
  title: 'CreatePlaylistButton',
  component: CreatePlaylistButton
} as Meta

export const Default: Story = () => <CreatePlaylistButton />
